# KloudCart Environment Configuration

# Flask Configuration
SECRET_KEY=your-secret-key-here
FLASK_ENV=development

# MongoDB Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/kloudcart

# Email Configuration (for PDF receipt sending)
# Gmail SMTP Configuration
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Alternative SMTP configurations:

# Outlook/Hotmail SMTP
# MAIL_SERVER=smtp-mail.outlook.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@outlook.com
# MAIL_PASSWORD=your-password

# Yahoo SMTP
# MAIL_SERVER=smtp.mail.yahoo.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@yahoo.com
# MAIL_PASSWORD=your-app-password

# Custom SMTP Server
# MAIL_SERVER=your-smtp-server.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@yourdomain.com
# MAIL_PASSWORD=your-password

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual credentials
# 3. For Gmail, use App Passwords instead of your regular password
# 4. Make sure to enable 2-factor authentication on your email account
# 5. Never commit the .env file to version control
